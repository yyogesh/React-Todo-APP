import React from 'react'

const DotNotation = {
    DatePicker: ({ color }) => {
        return <div> Color of date picker is :; {color}</div>
    },
    TextBox: ({ color }) => {
        return <div> Color of TextBox is :; {color}</div>
    }
}

export default DotNotation
